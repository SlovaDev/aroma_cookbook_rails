<div class="container" style="height:100%;">
  <div class="authentication">
    <div class="card">
      <div class="card-block">
      <h1><%= t('devise.mailer.password_change.header') %><%= @resource.email %></h1>

      <p><%= t('devise.mailer.pasword_change.instructions') %></p>

      <p><%= link_to t('devise.mailer.password_change.button'), edit_password_url(@resource, reset_password_token: @token) %></p>

      <p><%= t('devise.mailer.password_change.ignore') %></p>

      <p><%= t('devise.mailer.password_change.access') %></p>
      </div>
    </div>
  </div>
</div>
