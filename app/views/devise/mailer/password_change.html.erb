<div class="container" style="height:100%;">
  <div class="authentication">
    <div class="card">
      <div class="card-block">
      <h1><%= t('devise.mailer.password_change.header')%><%= @resource.email %></h1>

      <p><%= t('devise.mailer.password_change.instructions') %></p>

      <p><%= t('devise.mailer.password_change.detail') %></p>
      </div>
    </div>
  </div>
</div>
