<h1><%= @cuisine.name.pluralize %></h1>

<div class="row">
  <div class="col-md-8">
    <% @cuisine.recipes.each do |post| %>
        <div class="media">
          <div class="media-body">
            <h4 class="media-heading">
              <%= recipe.title %>
              <!-- <%= link_to recipe.title, cuisine_recipe_path(@cuisine, recipe) %> -->
            </h4>
          </div>
        </div>
      <% end %>
  </div>
</div>
